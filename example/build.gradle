apply plugin: 'android'

dependencies {
	compile rootProject
}

android {
	compileSdkVersion 7
	buildToolsVersion "19.0.1"

	sourceSets {
		main {
			manifest.srcFile 'AndroidManifest.xml'
			java.srcDirs = ['src']
			res.srcDirs = ['res']
		}
	}

}
